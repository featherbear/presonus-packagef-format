A self-contained HTML page which is the original proof of concept app before it was ported to Svelte.  
It doesn't use the Kaitai Struct file, but implements the data structure parsing on its own.  

File kept for funsies...
